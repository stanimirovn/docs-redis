---
title: Redis for Pivotal Platform
owner: London Services
---

<strong><%= modified_date %></strong>

<%= partial '../../redis/partials/branding-change' %>

<%= partial '../../redis/partials/pcf-name-change' %>

This is documentation for <%= vars.product_name %>.
You can download the <%= vars.product_name %> tile from [Pivotal Network](https://network.pivotal.io/products/p-redis).

This documentation:

+ Describes features and architecture of <%= vars.product_name %>.
+ Instructs the <%= vars.platform_name %> operator on how to install, configure, maintain, and backup <%= vars.product_name %>.
+ Instructs the app developer on how to choose a service plan, create and delete
Redis service instances, and bind an app.

##<a id="snapshot"></a> Product Snapshot

<table class="nice">
    <th>Element</th>
    <th>Details</th>
    <tr>
        <td>Version</td>
        <td>2.4.0</td>
    </tr>
    <tr>
        <td>Release date</td>
        <td>MMMM DD, 2020</td>
    </tr>
    <tr>
        <td>Software component version</td>
        <td>Redis OSS 5.0.7</td>
    </tr>
    <tr>
        <td>Compatible <%= vars.ops_manager %> version(s)</td>
        <td>2.7 and 2.8</td>
    </tr>
    <tr>
        <td>Compatible <%= vars.app_runtime_full %> version(s)</td>
        <td>2.7 and 2.8</td>
    </tr>
    <tr>
        <td>IaaS support</td>
        <td>AWS, Azure, GCP, OpenStack, and vSphere</td>
    </tr>
   <tr>
        <td>IPsec support</td>
        <td>Yes</td>
    </tr>
</table>

##<a id="about-redis"></a> About Redis

<strong><em>Redis</em></strong> is an easy to use, high speed key-value store that can be used as a database, cache, and message broker.
It supports a range of data structures including strings, lists, hashes, sets, bitmaps, hyperloglogs, and geospatial indexes.
It is easy to install and configure and is popular with engineers as a straightforward NoSQL data store.
It is used for everything from a quick way to store data for development and testing through to enterprise-scale apps like Twitter.

## <a id="about"></a> About <%= vars.product_name %>

<%= partial(:'../../redis/partials/redis/service-offerings', :locals => {
  :redis_name => "<strong><em>" + vars.product_name + "</em></strong>",
  :platform => vars.platform_name}) %>

### <a id="erc"></a> Is <%= vars.product_name %> right for your enterprise?

For information on recommended use cases, and the enterprise-readiness of <%= vars.product_name %>, see [Is <%= vars.product_name %> right for your enterprise?](./erc.html).

### <a id="upgrading"></a> Upgrading to the Latest Version

For information on how to upgrade and the supported upgrade paths, see [Upgrading <%= vars.product_name %>](./upgrade.html).

##<a id="more-info"></a> More Information

The following table lists where you can find topics related to the information on this page:

| For more information about&hellip; | See&hellip; |
|----------------------------|-------------|
|Product compatibility       |<a href="http://docs.pivotal.io/compatibility-matrix.pdf">Product Version Matrix</a>|
|How to upgrade <%= vars.product_name %>|<a href="upgrade.html">Upgrading <%= vars.product_name %></a>|
|How to use Redis |<a href="http://redis.io/documentation">Redis Documentation</a>|

##<a id="other-services"></a> <%= vars.product_name %> and Other <%= vars.platform_name %> Services

<%# The below partial is in https://github.com/pivotal-cf/docs-partials %>
<%= partial vars.path_to_partials + '/services/on-demand-service-table', :locals => {
  :service_name => vars.service_name, :platform_name => vars.platform_name, :rabbit => "RabbitMQ for Pivotal Platform", :redis => "Redis for Pivotal Platform", :mysql => "MySQL for Pivotal Platform"
} %>

##<a id="feedback"></a> Feedback

Please provide any bugs, feature requests, or questions to [the <%= vars.platform_name %> Feedback list](mailto:"pivotal-cf-feedback@pivotal.io").
